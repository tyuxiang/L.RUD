module check_player_module (
    input clk,  // clock
    input rst,  // reset
    output check_player,
    input answer_player,
    input player
  ) {
  
  always {
    if (player==answer_player){
      check_player=1;}
    else{check_player=0;}
    
  }
}