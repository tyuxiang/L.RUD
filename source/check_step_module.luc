module check_step_module (
    input lrud_player,
    input lrud_answer,
    output out
  ) {
  new_alu alu;
  always {
    out = hx;
    alu.firstNumber = lrud_player;
    alu.secondNumber = lrud_answer;
    alu.alufn = b100100;
    out = alu.answer[0];
  }
}
